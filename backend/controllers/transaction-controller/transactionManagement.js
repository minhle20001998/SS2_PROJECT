const productSchema = require("../../models/transactionSchema");
const mongoose = require("mongoose");

class transactionController {
    async getTransaction() {
        
    }
}

module.exports = new transactionController;
